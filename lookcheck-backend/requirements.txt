Flask>=3.0
flask-cors>=4.0
requests>=2.31
python-dotenv>=1.0
PyJWT>=2.8

# Production server (used by the Dockerfile / hosting platform)
gunicorn>=21.2

# PostgreSQL driver - only needed when DATABASE_URL points at Postgres.
# Local development on SQLite works without it.
psycopg[binary]>=3.1

# Image pipeline: garment extraction and catalogue-tile normalisation
Pillow>=10.2

# Background removal. Optional - without it the pipeline still crops,
# centres and normalises, it just keeps the original ground.
# The model (u2netp, 4.5MB) downloads on first use.
rembg[cpu]>=2.0.60
